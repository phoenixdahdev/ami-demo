import { SectionPlaceholder } from '@/components/home/section-placeholder';

export default function AccountsScreen() {
  return (
    <SectionPlaceholder
      title='Accounts'
      description='This tab is wired up and waiting on its design.'
    />
  );
}
