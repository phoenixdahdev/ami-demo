import { Stack } from 'expo-router';

/** The loan application: identity -> employment -> assessment -> offer. */
export default function LoanApplyLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
