export const HEIGHT = 48;
export const FONT_SIZE = 17;
export const BORDER_RADIUS = 26;
export const CORNERS = 999;

/**
 * Corner radii used across the FinTech kit. `BORDER_RADIUS` and `CORNERS`
 * above are the BNA defaults the component library ships with; these are what
 * the Figma screens actually draw.
 */
export const RADIUS = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  pill: 999,
};

export const SPACING = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};
